# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Outputs (regenerated by reproduce.sh)
figures/*.png
tables/*.csv
# NOTE: results/main_simulation.csv is intentionally committed so that
# reviewers can inspect the 4,500-run output without re-running the
# simulation. It is ~1 MB and is excluded from .gitignore.

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
