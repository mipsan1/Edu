# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Local debug scripts (one-off, not part of the published repo)
src/_debug_*.py
src/_archive_*.py

# Outputs (regenerated by reproduce.sh + counter_experiment.sh)
figures/*.png
tables/*.csv
# NOTE: results/*.csv is intentionally committed so that reviewers can
# inspect the 4,200-run main output AND the 14,000-row counter-experiment
# output without re-running the simulations.

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
